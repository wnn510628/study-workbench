# 个人 AI 学习工作台 — 交付报告

## 在线访问（正式长期部署）

**正式网址（GitHub Pages，长期免费 HTTPS）**：
https://wnn510628.github.io/study-workbench/

在桌面、平板、手机浏览器中打开即可使用。无需账号、无需后端。

> 2026-08-25 已从 CloudStudio 临时预览（会休眠）迁移至 GitHub Pages 正式部署，长期在线、免费、HTTPS 强制加密。
> 源码仓库：https://github.com/wnn510628/study-workbench

---

## 项目位置

本地源码目录：`C:\Users\admin\WorkBuddy\2026-08-25-11-21-37\study-workbench`

- `index.html` — 单页骨架
- `styles.css` — 视觉规范与响应式样式
- `data.js` — 内置示例数据（文章、场景、对话、摘抄、书籍、创作）
- `app.js` — 核心能力、路由、英语模块（单词本/阅读/听说/场景/对话/计划/摘抄）
- `app2.js` — 中文模块（读书计划/创作板块）、收集箱、搜索、AI、设置与数据
- `.verify/verify.js` — 自动化验证脚本

---

## 运行方式

1. **在线**：直接访问上方分享链接。
2. **本地离线**：用任意现代浏览器打开 `index.html`，或在本目录运行静态服务器：
   ```bash
   python -m http.server 8000
   ```
   然后访问 `http://localhost:8000`。

无需账号、无需后端、无需构建。

---

## 数据存储说明

- **数据保存在当前浏览器本地**：
  - 文本数据：浏览器 `localStorage`（键 `wb_data_v1`）。
  - 录音音频：浏览器 `IndexedDB`（数据库 `wb_audio`）。
- **不会自动云同步**。换设备、换浏览器、清空缓存后数据会丢失。
- 请在 **「设置与数据」** 中定期 **导出 JSON 备份**；需要恢复时通过 **导入** 还原。
- 导出备份包含录音元数据，不包含录音音频本体；音频仍保留在导出时所在的浏览器里。

---

## 账号与费用

| 项目 | 说明 |
|---|---|
| 本工作台 | **完全免费**，无订阅、无广告、无后端服务。 |
| 在线托管 | **GitHub Pages 长期免费托管**（https://wnn510628.github.io/study-workbench/），HTTPS 强制加密，不会休眠。 |
| AI 对话 / AI 帮手 | 当前版本为 **界面预留**。需要你在 **「设置与数据 → AI 配置」** 中填写自己的 OpenAI 兼容接口（BaseURL、API Key、模型）后才能启用；费用由你的 API 服务商按实际用量收取。 |

---

## 已实现功能

### 今日
- 欢迎卡片、待确认收集提示、今日任务、待复习单词。
- 一键「生成今日计划」，自动聚合各模块数据。
- 顶部标题 / 副标题支持自定义编辑（未自定义时显示默认值）。

### 英语学习
- **单词本**：记录生词，支持首字母筛选、中文释义反查、编辑删除、复习状态、间隔重复。
- **英文阅读**：内置 4 篇精选短文；支持添加自定义文章、原文标注/划线/摘抄、长难句解析、背诵背写模式。
- **听说训练**：使用麦克风录音，音频保存到本地 IndexedDB；支持播放与删除。
- **场景英语**：7 个生活/工作场景，含常用句型和场景对话。
- **对话练习**：内置多组场景对话，可逐句跟读；AI 自由对话已预留界面。
- **学习计划**：每日计划 + 每周复习计划，可手动添加任务。

### 中文学习
- **佳句摘抄**：记录中文优美词句，支持标签与感悟。
- **读书计划**：管理书单、进度、读书笔记。
- **创作板块**：独立的灵感收集与作品管理。

### 公共能力
- **收集箱**：统一入口；输入后自动建议中英文分类，低置信度时手动选择模块确认归入。
- **搜索**：跨模块全局搜索（单词、文章、摘抄、书籍、创作、灵感、收集箱、场景、对话）。
- **AI 帮手**：界面预留；配置 API 后支持整理资料、补充知识、安排任务。
- **设置与数据**：使用说明、AI 配置、数据导出/导入/重置、数据摘要、更新日志。

### 交互与响应式
- 桌面侧边栏、平板适配、手机底部导航 + 更多抽屉 + 侧边抽屉。
- 验证视口：360×800、375×812、390×844、1024×768、1280×800。
- 帮助入口每个模块都有；点击导航不再误触发帮助弹层。

---

## 界面预留 / 待配置后可用

| 能力 | 当前状态 |
|---|---|
| AI 对话练习 | 界面就绪，需配置 API 后启用。 |
| AI 补充知识 | 界面就绪，需配置 API 后启用。 |
| AI 安排任务 | 界面就绪，需配置 API 后启用。 |
| 云同步 | 不在第一版范围；数据通过 JSON 导出/导入迁移。 |

---

## 内置示例与扩展

- 内置 4 篇英文短文、7 个场景、3 组对话、中文摘抄/书籍/创作示例。
- 所有内置内容均可通过对应模块的「添加」按钮自行扩充；也可直接编辑 `data.js` 后重新部署。

---

## 验证结果

自动化验证 52 / 52 项通过，0 控制台错误，0 页面异常。关键修复记录：
- 修复 `[hidden]` 属性被 CSS `display: flex` 覆盖导致的弹层遮罩残留问题。
- 修复带 `data-help` 的导航项被误拦截导致页面不跳转的问题。
- 按你最新确认移除「四六级 / 专业英语 / 雅思托福词汇库」，仅保留生词记录（单词本）。
- 移动端添加/编辑单词改为字典式紧凑呈现（参考词典 App）：顶部大字号单词 + 音标，下方一个释义文本区按行填写，不再单独展示词性/例句输入框；桌面端保持完整表单。
- 今日页顶部标题 / 副标题支持自定义，未设置时恢复默认值。

## 后续更新（2026-08-25）

- **修复手机端侧边栏无法点击**：侧边抽屉 z-index 由 40 提升到 60（高于遮罩层 50），打开后导航项可正常点击进入，跳转后抽屉自动关闭。验证 16 / 16 项通过。
- **新增工作台名称自定义**：「设置与数据 → 工作台」可修改工作台名称（最多 30 字），保存后立即生效于侧边栏品牌区、浏览器标签页，并随数据持久化、包含在导出备份中。

## 正式部署记录（2026-08-25 17:00）

- 部署方式：**GitHub Pages**（仓库 `wnn510628/study-workbench`，公开，main 分支 / 根目录）。
- 正式网址：https://wnn510628.github.io/study-workbench/（HTTP 200，5 个静态资源全部验证通过）。
- 数据说明：页面数据仍保存在 **访问者浏览器本地**（localStorage + IndexedDB），**不做云端同步**；换设备请用「设置与数据 → 导出/导入 JSON 备份」迁移。
- 更新方式：修改本地 `study-workbench` 目录文件后 `git add / commit / push` 到 GitHub 即自动重新部署。
